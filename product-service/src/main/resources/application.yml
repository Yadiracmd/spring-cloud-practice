server:
  port: 8030

spring:
  application:
    name: product-service

  profiles:
    active: dev

  cloud:
    nacos:
      discovery:
        server-addr: localhost:7001
        username: nacos
        password: nacos